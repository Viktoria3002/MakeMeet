<% content_for :title, "Редактировать комментарий - Админ панель" %>

<div class="admin-container">
  <div class="admin-header">
    <h1>✏️ Редактировать комментарий</h1>
  </div>

  <%= form_with model: [:admin, @comment], url: admin_update_comment_path(@comment), method: :patch, local: true do |f| %>
    <div class="form-group">
      <%= f.label :content, "Содержание комментария", class: "form-label" %>
      <%= f.text_area :content, class: "form-input", rows: 6, required: true, style: "resize: vertical;" %>
    </div>

    <div class="form-group">
      <%= f.label :post_id, "Пост", class: "form-label" %>
      <%= f.collection_select :post_id, @posts, :id, :id, {}, { class: "form-input" } %>
    </div>

    <div class="form-group">
      <%= f.label :author_id, "Автор", class: "form-label" %>
      <%= f.collection_select :author_id, @users, :id, :username, {}, { class: "form-input" } %>
    </div>

    <div style="display: flex; gap: 10px; margin-top: 30px;">
      <%= f.submit "Сохранить изменения", class: "admin-login-submit" %>
      <%= link_to "Отмена", admin_comments_path, class: "btn", style: "background: rgba(255, 255, 255, 0.1); color: rgba(255, 255, 255, 0.7);" %>
    </div>
  <% end %>
</div>

